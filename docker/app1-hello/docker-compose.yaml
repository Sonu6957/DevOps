services:
  hello-py:
    build: ./python
    ports: 
      - "3000:3000"
    environment:
      - ENV_VALUE=${ENV_VALUE}

  hello-node:
    build: ./node
    ports: 
      - "3001:5000"
    environment:
      - ENV_VALUE=${ENV_VALUE_SPRING}