name: Usage of dependency between workflow
on: workflow_dispatch
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            -
                name: First step
                run: echo "Execution of first step"

           
    test:
        runs-on: ubuntu-latest
        needs: build
        steps:
            -
                name: Run testing
                run: echo "running the test command"
